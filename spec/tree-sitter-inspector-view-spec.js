'use babel';

import TreeSitterInspectorView from '../lib/tree-sitter-inspector-view';

describe('TreeSitterInspectorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
